//go:build !linux

package procmatch

// На прочих платформах определение процесса пока не реализовано.
//
// Правило `process:` при этом не ломается и не молчит: оно просто не срабатывает, а клиент
// говорит об этом при старте — так же, как о правилах geosite без баз. На Windows источником
// станет таблица соединений из iphlpapi, и подставится он этим же интерфейсом.
func New() Finder { return nopFinder{} }
